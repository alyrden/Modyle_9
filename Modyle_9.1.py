def apply_all_func(self, int_list, *functions, sum, len, sorted):
    self.int_list = list(int, float(int_list))
    self.__functions = map(functions)
    return dict{name:int_list, result}

def apply_all_func(self, result, functions=None):
    self.result = functions
    for i in result:
        return result

print(apply_all_func([6, 20, 15, 9], max, min))
print(apply_all_func([6, 20, 15, 9], len, sum, sorted))